# BriefScope CLOUD — no API keys here.
# Open the plugin UI → Settings to enter your cloud provider key.
# Keys are saved to /data/config.json (persists across restarts).

# These two are set automatically by docker-compose — do not change:
# ROOT_PATH=/api/briefscope_cloud_openai
# LLM_MODE=cloud
